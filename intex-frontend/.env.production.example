# Used at build time (Vite). Copy to `.env.production` for local prod builds, or set the same name in GitHub Actions / Azure SWA build settings.
# Option A: main site = this frontend; API = separate origin (e.g. https://api.yourdomain.com).
VITE_API_BASE_URL=https://api.example.com
